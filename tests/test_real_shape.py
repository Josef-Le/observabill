"""Regression tests for real Datadog logs-aggregate response shape.

NOTE: The shallow detector functions detect_exclusion_candidates and _is_noise_status
were removed in the v2 content-intelligence refactor. This file is intentionally
empty — it exists only to confirm the old tests do not accidentally run.
"""
